- route:
    id: query-invoices
    from:
      uri: timer:queryInvoices
      parameters:
        repeatCount: 1
      steps:
        - to:
            uri: sql:SELECT * FROM invoices
        - log:
            message: "${body}"
# - beans:
#     - name: dataSource
#       type: org.springframework.jdbc.datasource.DriverManagerDataSource
#       properties:
#         driverClassName: org.postgresql.Driver
#         url: jdbc:postgresql://postgres.user1-database.svc:5432/test
#         username: user1
#         password: user1